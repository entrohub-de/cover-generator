{
  "builds": [
    {
      "src": "api/generate.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": "fonts/**,public/logo.png"
      }
    },
    {
      "src": "public/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/api/generate", "dest": "/api/generate.py" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
